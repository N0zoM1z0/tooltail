<Project>
  <Import Project="../Directory.Build.props" />

  <PropertyGroup>
    <!-- Locks the sole v0.1 distribution RID without changing portable TargetFrameworks. -->
    <RuntimeIdentifiers>win-x64</RuntimeIdentifiers>
  </PropertyGroup>
</Project>
